class Event {
  List<int> userId;
  String eventId;
  String name;
  String description;
  DateTime dateTime;
  int size;

  Event(this.userId, this.eventId, this.name, this.description, this.dateTime, this.size);
}