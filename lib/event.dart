class Event {
  List<int> userId;
  String eventId;
  String name;
  String description;
  DateTime dateTime;
  int size;
  String owner;

  Event(this.userId, this.eventId, this.name, this.description, this.dateTime, this.size, this.owner);
}