
class Profile {
  String name;
  String id;
  int age;
  String bio;
  String pfp; // TODO: make a temp folder that contains profile pictures

  Profile(this.name, this.id, this.age, this.bio, this.pfp);
}